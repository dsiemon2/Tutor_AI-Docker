services:
  nginx:
    image: nginx:alpine
    container_name: tutorai-old-nginx
    ports:
      - "8092:80"
    volumes:
      - ./docker/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
      - admin
    restart: unless-stopped
    networks:
      - tutorai-old-network

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tutorai-old-app
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/dev.db
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ADMIN_TOKEN=${ADMIN_TOKEN:-admin}
      - SESSION_SECRET=${SESSION_SECRET:-change-me-in-production}
      - PORT=3000
    volumes:
      - db-data-old:/app/data
    command: ["node", "dist/server.js"]
    restart: unless-stopped
    networks:
      - tutorai-old-network

  admin:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tutorai-old-admin
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/dev.db
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ADMIN_TOKEN=${ADMIN_TOKEN:-admin}
      - SESSION_SECRET=${SESSION_SECRET:-change-me-in-production}
      - ADMIN_PORT=3001
    volumes:
      - db-data-old:/app/data
    command: ["node", "dist/adminServer.js"]
    restart: unless-stopped
    networks:
      - tutorai-old-network

volumes:
  db-data-old:

networks:
  tutorai-old-network:
    driver: bridge
