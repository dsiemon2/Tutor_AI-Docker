<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; }
    .sidebar { min-height: 100vh; max-height: 100vh; overflow-y: auto; position: sticky; top: 0; }
    .sidebar .nav-link { color: rgba(255,255,255,0.8); padding: 0.75rem 1rem; }
    .sidebar .nav-link:hover, .sidebar .nav-link.active { color: #fff; background: rgba(255,255,255,0.1); }
    .sidebar .nav-link i { margin-right: 0.5rem; }
    .main-content { background: #f8f9fa; min-height: 100vh; }
    .nav-divider { border-top: 1px solid rgba(255,255,255,0.2); margin: 0.5rem 0; }
    .nav-section { color: rgba(255,255,255,0.5); font-size: 0.75rem; padding: 0.5rem 1rem; text-transform: uppercase; }
    .card { border: none; box-shadow: 0 0.125rem 0.25rem rgba(0,0,0,0.075); }
    .difficulty-1 { color: #22c55e; }
    .difficulty-2 { color: #84cc16; }
    .difficulty-3 { color: #eab308; }
    .difficulty-4 { color: #f97316; }
    .difficulty-5 { color: #ef4444; }
  </style>
</head>
<body>
  <div class="container-fluid">
    <div class="row">
      <%- include('_sidebar', { token, active: 'subjects', branding, basePath }) %>

      <main class="col-md-10 ms-sm-auto main-content p-4">
        <!-- Breadcrumb -->
        <nav aria-label="breadcrumb" class="mb-3">
          <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="<%= basePath %>/admin?token=<%= token %>">Admin</a></li>
            <li class="breadcrumb-item"><a href="<%= basePath %>/admin/subjects?token=<%= token %>">Subjects</a></li>
            <li class="breadcrumb-item"><a href="<%= basePath %>/admin/subjects/<%= topic.subject.id %>/edit?token=<%= token %>"><%= topic.subject.name %></a></li>
            <li class="breadcrumb-item"><a href="<%= basePath %>/admin/subjects/<%= topic.subject.id %>/topics?token=<%= token %>">Topics</a></li>
            <li class="breadcrumb-item active"><%= topic.name %></li>
          </ol>
        </nav>

        <div class="d-flex justify-content-between align-items-center mb-4">
          <div>
            <h2><i class="bi bi-pencil text-primary me-2"></i>Edit Topic</h2>
            <p class="text-muted mb-0">
              <span class="badge bg-primary me-1"><%= topic.subject.category?.icon || '' %> <%= topic.subject.category?.name %></span>
              <span class="badge bg-secondary"><%= topic.subject.name %></span>
            </p>
          </div>
          <a href="<%= basePath %>/admin/subjects/<%= topic.subject.id %>/topics?token=<%= token %>" class="btn btn-secondary">
            <i class="bi bi-arrow-left me-1"></i>Back to Topics
          </a>
        </div>

        <div class="row">
          <div class="col-lg-8">
            <div class="card">
              <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-list-check me-2"></i>Topic Details</h5>
              </div>
              <div class="card-body">
                <form action="<%= basePath %>/admin/topics/<%= topic.id %>?token=<%= token %>" method="POST">
                  <div class="row g-3">
                    <div class="col-md-6">
                      <label class="form-label">Code <span class="text-danger">*</span></label>
                      <input type="text" name="code" class="form-control font-monospace" value="<%= topic.code %>" required pattern="[A-Za-z0-9_-]+" title="Letters, numbers, underscores, and hyphens only">
                      <small class="text-muted">Unique identifier for this topic</small>
                    </div>
                    <div class="col-md-6">
                      <label class="form-label">Name <span class="text-danger">*</span></label>
                      <input type="text" name="name" class="form-control" value="<%= topic.name %>" required>
                    </div>
                    <div class="col-md-4">
                      <label class="form-label">Grade Level</label>
                      <select name="gradeLevel" class="form-select">
                        <option value="" <%= topic.gradeLevel === null ? 'selected' : '' %>>Not specified</option>
                        <option value="-2" <%= topic.gradeLevel === -2 ? 'selected' : '' %>>Pre-K 3</option>
                        <option value="-1" <%= topic.gradeLevel === -1 ? 'selected' : '' %>>Pre-K 4</option>
                        <option value="0" <%= topic.gradeLevel === 0 ? 'selected' : '' %>>Kindergarten</option>
                        <% for (let g = 1; g <= 12; g++) { %>
                        <option value="<%= g %>" <%= topic.gradeLevel === g ? 'selected' : '' %>>Grade <%= g %></option>
                        <% } %>
                      </select>
                    </div>
                    <div class="col-md-4">
                      <label class="form-label">Difficulty (1-5)</label>
                      <select name="difficulty" class="form-select">
                        <option value="1" <%= topic.difficulty === 1 ? 'selected' : '' %>>1 - Very Easy</option>
                        <option value="2" <%= topic.difficulty === 2 ? 'selected' : '' %>>2 - Easy</option>
                        <option value="3" <%= topic.difficulty === 3 ? 'selected' : '' %>>3 - Medium</option>
                        <option value="4" <%= topic.difficulty === 4 ? 'selected' : '' %>>4 - Hard</option>
                        <option value="5" <%= topic.difficulty === 5 ? 'selected' : '' %>>5 - Very Hard</option>
                      </select>
                    </div>
                    <div class="col-md-4">
                      <label class="form-label">Order</label>
                      <input type="number" name="order" class="form-control" value="<%= topic.order %>" min="0">
                      <small class="text-muted">Display order within subject</small>
                    </div>
                    <div class="col-12">
                      <label class="form-label">Description</label>
                      <textarea name="description" class="form-control" rows="4" placeholder="What does this topic cover? What will students learn?"><%= topic.description || '' %></textarea>
                    </div>
                    <div class="col-12">
                      <div class="form-check form-switch">
                        <input type="checkbox" name="isActive" class="form-check-input" id="isActive" <%= topic.isActive ? 'checked' : '' %>>
                        <label class="form-check-label" for="isActive">
                          <strong>Active</strong>
                          <br><small class="text-muted">Inactive topics are hidden from students</small>
                        </label>
                      </div>
                    </div>
                  </div>

                  <hr class="my-4">

                  <div class="d-flex justify-content-between">
                    <a href="<%= basePath %>/admin/subjects/<%= topic.subject.id %>/topics?token=<%= token %>" class="btn btn-outline-secondary">
                      <i class="bi bi-x-lg me-1"></i>Cancel
                    </a>
                    <button type="submit" class="btn btn-primary">
                      <i class="bi bi-check-lg me-1"></i>Save Changes
                    </button>
                  </div>
                </form>
              </div>
            </div>
          </div>

          <div class="col-lg-4">
            <!-- Topic Info Card -->
            <div class="card mb-4">
              <div class="card-header">
                <h6 class="mb-0"><i class="bi bi-info-circle me-2"></i>Topic Info</h6>
              </div>
              <div class="card-body">
                <table class="table table-sm mb-0">
                  <tr>
                    <td class="text-muted">Subject</td>
                    <td><span class="badge bg-primary"><%= topic.subject.name %></span></td>
                  </tr>
                  <tr>
                    <td class="text-muted">Category</td>
                    <td><%= topic.subject.category?.icon || '' %> <%= topic.subject.category?.name %></td>
                  </tr>
                  <tr>
                    <td class="text-muted">Difficulty</td>
                    <td>
                      <span class="difficulty-<%= Math.min(topic.difficulty, 5) %>">
                        <% for (let i = 0; i < topic.difficulty; i++) { %>
                          <i class="bi bi-star-fill"></i>
                        <% } %>
                        <% for (let i = topic.difficulty; i < 5; i++) { %>
                          <i class="bi bi-star"></i>
                        <% } %>
                      </span>
                    </td>
                  </tr>
                  <tr>
                    <td class="text-muted">Status</td>
                    <td>
                      <% if (topic.isActive) { %>
                        <span class="badge bg-success">Active</span>
                      <% } else { %>
                        <span class="badge bg-secondary">Inactive</span>
                      <% } %>
                    </td>
                  </tr>
                  <tr>
                    <td class="text-muted">Created</td>
                    <td><%= new Date(topic.createdAt).toLocaleDateString() %></td>
                  </tr>
                  <tr>
                    <td class="text-muted">Updated</td>
                    <td><%= new Date(topic.updatedAt).toLocaleDateString() %></td>
                  </tr>
                </table>
              </div>
            </div>

            <!-- Quick Actions Card -->
            <div class="card">
              <div class="card-header">
                <h6 class="mb-0"><i class="bi bi-lightning me-2"></i>Quick Actions</h6>
              </div>
              <div class="card-body">
                <div class="d-grid gap-2">
                  <a href="<%= basePath %>/admin/subjects/<%= topic.subject.id %>/topics?token=<%= token %>" class="btn btn-outline-primary">
                    <i class="bi bi-arrow-left me-1"></i>Back to Topics List
                  </a>
                  <a href="<%= basePath %>/admin/subjects/<%= topic.subject.id %>/edit?token=<%= token %>" class="btn btn-outline-secondary">
                    <i class="bi bi-book me-1"></i>Edit Subject
                  </a>
                  <hr class="my-2">
                  <form action="<%= basePath %>/admin/topics/<%= topic.id %>/delete?token=<%= token %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this topic? This action cannot be undone.');">
                    <button type="submit" class="btn btn-outline-danger w-100">
                      <i class="bi bi-trash me-1"></i>Delete Topic
                    </button>
                  </form>
                </div>
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
