<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Logic Rules - TutorAI Admin</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; }
    .sidebar { min-height: 100vh; max-height: 100vh; overflow-y: auto; position: sticky; top: 0; }
    .sidebar .nav-link { color: rgba(255,255,255,0.8); padding: 0.75rem 1rem; }
    .sidebar .nav-link:hover, .sidebar .nav-link.active { color: #fff; background: rgba(255,255,255,0.1); }
    .sidebar .nav-link i { margin-right: 0.5rem; }
    .main-content { background: #f8f9fa; min-height: 100vh; }
    .nav-divider { border-top: 1px solid rgba(255,255,255,0.2); margin: 0.5rem 0; }
    .nav-section { color: rgba(255,255,255,0.5); font-size: 0.75rem; padding: 0.5rem 1rem; text-transform: uppercase; }
    .card { border: none; box-shadow: 0 0.125rem 0.25rem rgba(0,0,0,0.075); }
    .rule-card { border-left: 4px solid #dee2e6; transition: all 0.2s; }
    .rule-card:hover { border-left-color: #0ea5e9; }
    .rule-card.active { border-left-color: #198754; }
    .rule-card.inactive { border-left-color: #6c757d; }
  </style>
</head>
<body>
  <div class="container-fluid">
    <div class="row">
      <%- include('_sidebar', { token, active: 'logic-rules', branding, basePath }) %>

      <main class="col-md-10 ms-sm-auto main-content p-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
          <h2><i class="bi bi-diagram-3 text-primary me-2"></i>Logic Rules</h2>
          <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addRuleModal">
            <i class="bi bi-plus-lg me-1"></i>Add Rule
          </button>
        </div>

        <p class="text-muted mb-4">Define conditional logic rules that control AI tutor behavior based on student interactions.</p>

        <!-- Stats Row -->
        <div class="row g-3 mb-4">
          <div class="col-md-4">
            <div class="card text-center">
              <div class="card-body">
                <i class="bi bi-diagram-3 text-primary fs-1"></i>
                <h3 class="mt-2"><%= rules ? rules.length : 0 %></h3>
                <small class="text-muted">Total Rules</small>
              </div>
            </div>
          </div>
          <div class="col-md-4">
            <div class="card text-center">
              <div class="card-body">
                <i class="bi bi-check-circle text-success fs-1"></i>
                <h3 class="mt-2"><%= rules ? rules.filter(r => r.isActive).length : 0 %></h3>
                <small class="text-muted">Active Rules</small>
              </div>
            </div>
          </div>
          <div class="col-md-4">
            <div class="card text-center">
              <div class="card-body">
                <i class="bi bi-pause-circle text-secondary fs-1"></i>
                <h3 class="mt-2"><%= rules ? rules.filter(r => !r.isActive).length : 0 %></h3>
                <small class="text-muted">Inactive Rules</small>
              </div>
            </div>
          </div>
        </div>

        <div class="row g-4">
          <% if (rules && rules.length > 0) { %>
            <% rules.forEach(rule => { %>
              <div class="col-md-6">
                <div class="card rule-card h-100 <%= rule.isActive ? 'active' : 'inactive' %>">
                  <div class="card-body">
                    <div class="d-flex justify-content-between align-items-start mb-3">
                      <h5>
                        <i class="bi bi-lightning text-warning me-2"></i>
                        <%= rule.name %>
                      </h5>
                      <% if (rule.isActive) { %>
                        <span class="badge bg-success">Active</span>
                      <% } else { %>
                        <span class="badge bg-secondary">Inactive</span>
                      <% } %>
                    </div>
                    <% if (rule.description) { %>
                      <p class="text-muted small mb-3"><%= rule.description %></p>
                    <% } %>
                    <div class="bg-light rounded p-3 mb-3">
                      <small class="text-muted">IF</small>
                      <p class="mb-1"><code><%= rule.condition %></code></p>
                      <small class="text-muted">THEN</small>
                      <p class="mb-0"><code><%= rule.action %></code></p>
                    </div>
                    <div class="d-flex justify-content-between align-items-center">
                      <small class="text-muted">Priority: <span class="badge bg-<%= rule.priority >= 8 ? 'danger' : (rule.priority >= 5 ? 'warning text-dark' : 'info') %>"><%= rule.priority %></span></small>
                      <small class="text-muted">Updated: <%= new Date(rule.updatedAt).toLocaleDateString() %></small>
                    </div>
                  </div>
                  <div class="card-footer bg-transparent">
                    <button class="btn btn-sm btn-outline-primary" data-bs-toggle="modal" data-bs-target="#editRuleModal<%= rule.id %>">
                      <i class="bi bi-pencil"></i> Edit
                    </button>
                    <form action="<%= basePath %>/admin/logic-rules/<%= rule.id %>/delete?token=<%= token %>" method="POST" class="d-inline" onsubmit="return confirm('Delete this rule?');">
                      <button type="submit" class="btn btn-sm btn-outline-danger">
                        <i class="bi bi-trash"></i> Delete
                      </button>
                    </form>
                  </div>
                </div>
              </div>
            <% }) %>
          <% } else { %>
            <div class="col-12">
              <div class="card">
                <div class="card-body text-center py-5 text-muted">
                  <i class="bi bi-diagram-3 fs-1 d-block mb-3"></i>
                  <h5>No Logic Rules Yet</h5>
                  <p>Click "Add Rule" to create your first adaptive learning rule.</p>
                </div>
              </div>
            </div>
          <% } %>
        </div>
      </main>
    </div>
  </div>

  <!-- Add Rule Modal -->
  <div class="modal fade" id="addRuleModal" tabindex="-1">
    <div class="modal-dialog modal-lg">
      <div class="modal-content">
        <form action="<%= basePath %>/admin/logic-rules?token=<%= token %>" method="POST">
          <div class="modal-header">
            <h5 class="modal-title"><i class="bi bi-plus-lg me-2"></i>Add Logic Rule</h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
          </div>
          <div class="modal-body">
            <div class="row g-3">
              <div class="col-12">
                <label class="form-label">Rule Name <span class="text-danger">*</span></label>
                <input type="text" name="name" class="form-control" placeholder="e.g., Struggling Student Support" required>
              </div>
              <div class="col-12">
                <label class="form-label">Description</label>
                <input type="text" name="description" class="form-control" placeholder="Brief description of what this rule does">
              </div>
              <div class="col-12">
                <label class="form-label">Condition (IF) <span class="text-danger">*</span></label>
                <textarea name="condition" class="form-control font-monospace" rows="2" placeholder="e.g., student.incorrectAnswers >= 3 AND topic.same == true" required></textarea>
                <small class="text-muted">Use JavaScript-like syntax. Available: student.*, session.*, topic.*, message.*</small>
              </div>
              <div class="col-12">
                <label class="form-label">Action (THEN) <span class="text-danger">*</span></label>
                <textarea name="action" class="form-control font-monospace" rows="2" placeholder="e.g., switchToSupportiveMode(); offerAlternativeExplanation();" required></textarea>
                <small class="text-muted">Actions: switchToSupportiveMode(), increaseDifficulty(), suggestBreak(), celebrateAchievement(), etc.</small>
              </div>
              <div class="col-md-6">
                <label class="form-label">Priority (0-10)</label>
                <input type="number" name="priority" class="form-control" value="5" min="0" max="10">
                <small class="text-muted">Higher priority rules are evaluated first</small>
              </div>
            </div>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-primary">Save Rule</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Edit Modals for each rule -->
  <% if (rules) { %>
    <% rules.forEach(rule => { %>
      <div class="modal fade" id="editRuleModal<%= rule.id %>" tabindex="-1">
        <div class="modal-dialog modal-lg">
          <div class="modal-content">
            <form action="<%= basePath %>/admin/logic-rules/<%= rule.id %>?token=<%= token %>" method="POST">
              <div class="modal-header">
                <h5 class="modal-title"><i class="bi bi-pencil me-2"></i>Edit Logic Rule</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
              </div>
              <div class="modal-body">
                <div class="row g-3">
                  <div class="col-md-8">
                    <label class="form-label">Rule Name <span class="text-danger">*</span></label>
                    <input type="text" name="name" class="form-control" value="<%= rule.name %>" required>
                  </div>
                  <div class="col-md-4">
                    <label class="form-label">Status</label>
                    <div class="form-check form-switch mt-2">
                      <input type="checkbox" name="isActive" class="form-check-input" id="active<%= rule.id %>" <%= rule.isActive ? 'checked' : '' %>>
                      <label class="form-check-label" for="active<%= rule.id %>">Active</label>
                    </div>
                  </div>
                  <div class="col-12">
                    <label class="form-label">Description</label>
                    <input type="text" name="description" class="form-control" value="<%= rule.description || '' %>">
                  </div>
                  <div class="col-12">
                    <label class="form-label">Condition (IF) <span class="text-danger">*</span></label>
                    <textarea name="condition" class="form-control font-monospace" rows="2" required><%= rule.condition %></textarea>
                  </div>
                  <div class="col-12">
                    <label class="form-label">Action (THEN) <span class="text-danger">*</span></label>
                    <textarea name="action" class="form-control font-monospace" rows="2" required><%= rule.action %></textarea>
                  </div>
                  <div class="col-md-6">
                    <label class="form-label">Priority (0-10)</label>
                    <input type="number" name="priority" class="form-control" value="<%= rule.priority %>" min="0" max="10">
                  </div>
                </div>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="submit" class="btn btn-primary">Save Changes</button>
              </div>
            </form>
          </div>
        </div>
      </div>
    <% }) %>
  <% } %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
