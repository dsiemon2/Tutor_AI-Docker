<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    :root {
      --primary-color: <%= branding.primaryColor || '#0ea5e9' %>;
      --secondary-color: <%= branding.secondaryColor || '#0284c7' %>;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    .auth-container { width: 100%; max-width: 450px; }

    .auth-card {
      background: white;
      border-radius: 16px;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
      overflow: hidden;
    }

    .auth-header {
      padding: 2rem 2rem 1.5rem;
      text-align: center;
      border-bottom: 1px solid #e2e8f0;
    }

    .auth-header .logo { font-size: 3rem; margin-bottom: 0.5rem; }
    .auth-header .logo.success { color: #10b981; }
    .auth-header .logo.error { color: #ef4444; }
    .auth-header h1 { font-size: 1.5rem; font-weight: 700; color: #1e293b; margin: 0; }
    .auth-header p { color: #64748b; margin: 0.5rem 0 0; }

    .auth-body { padding: 2rem; }

    .btn-primary {
      background: var(--primary-color);
      border-color: var(--primary-color);
      padding: 0.75rem 1.5rem;
      font-weight: 600;
      border-radius: 8px;
    }

    .btn-primary:hover {
      background: var(--secondary-color);
      border-color: var(--secondary-color);
    }

    .btn-success {
      padding: 0.75rem 1.5rem;
      font-weight: 600;
      border-radius: 8px;
    }

    .auth-footer {
      padding: 1.5rem 2rem;
      background: #f8fafc;
      text-align: center;
      border-top: 1px solid #e2e8f0;
    }

    .auth-footer a { color: var(--primary-color); text-decoration: none; font-weight: 500; }
    .auth-footer a:hover { text-decoration: underline; }

    .features-list {
      background: #f0fdf4;
      border-radius: 12px;
      padding: 1.5rem;
      margin: 1.5rem 0;
    }

    .features-list h6 { color: #166534; margin-bottom: 1rem; }
    .features-list ul { margin: 0; padding-left: 1.25rem; }
    .features-list li { color: #15803d; margin-bottom: 0.5rem; }
  </style>
</head>
<body>
  <div class="auth-container">
    <div class="auth-card">
      <% if (success) { %>
        <div class="auth-header">
          <div class="logo success"><i class="bi bi-check-circle-fill"></i></div>
          <h1>Email Verified!</h1>
          <p>Your account is now active</p>
        </div>

        <div class="auth-body text-center">
          <div class="alert alert-success mb-4">
            <i class="bi bi-check-circle me-2"></i><%= success %>
          </div>

          <div class="features-list text-start">
            <h6><i class="bi bi-stars me-2"></i>You now have access to:</h6>
            <ul>
              <li>AI-powered tutoring in 74+ subjects</li>
              <li>Voice and text learning modes</li>
              <li>Progress tracking and analytics</li>
              <li>Interactive quizzes and assignments</li>
            </ul>
          </div>

          <a href="<%= typeof basePath !== 'undefined' ? basePath : '' %>/auth/login" class="btn btn-success w-100">
            <i class="bi bi-box-arrow-in-right me-2"></i>Sign In to Start Learning
          </a>
        </div>
      <% } else { %>
        <div class="auth-header">
          <div class="logo error"><i class="bi bi-x-circle-fill"></i></div>
          <h1>Verification Failed</h1>
          <p>We couldn't verify your email</p>
        </div>

        <div class="auth-body">
          <div class="alert alert-danger mb-4">
            <i class="bi bi-exclamation-circle me-2"></i><%= error %>
          </div>

          <p class="text-muted mb-4">
            This can happen if the link has expired or has already been used.
            You can request a new verification email below.
          </p>

          <a href="<%= typeof basePath !== 'undefined' ? basePath : '' %>/auth/login" class="btn btn-primary w-100 mb-2">
            <i class="bi bi-box-arrow-in-right me-2"></i>Go to Login
          </a>
          <p class="text-center text-muted mt-3">
            <small>Log in and request a new verification email from your account settings.</small>
          </p>
        </div>
      <% } %>

      <div class="auth-footer">
        Need help? <a href="mailto:support@tutorai.com">Contact Support</a>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
